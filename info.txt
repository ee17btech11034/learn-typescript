// TypeScript with React :==> 

    -- Setup:
        -- install
                -- `npm create vite@latest`
                        -- React > react + tS > yes (package install or else `npm i` after all done)
                        