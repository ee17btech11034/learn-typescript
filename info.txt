# TypeScript:==>
    -- TS provides extra layer of security. 
    -- extension is ".ts".
    -- TS   =    JS + Types (Data Types)


    -- TS Setup:
        -- We can install TS, Globally or Locally (project wise). 
            -- Better for project wise, as we may work on different versions.

        -- 1. Traditional Setup way:
                    -- `npm init -y`   [we can use bun as well]
                    -- `npm i -D typescript`
                            -- we can user typescript with node the       @types/node 
                            -- when ts with react      then       @types/react
                    -- `npx tsc --init`
                        -- initialize a config using tsc (ts compiler)
                        -- uncomment      `rootdir, outdir` 
                                -- create a "dist" folder for outdir
                                -- create a "src" folder for rootdir
                        -- go though recommended options, etc.
                        -- Go to TS playground and then check these TS Config.

                    
                    -- create a .ts file in src. 
                    -- `npx tsc` 
                        -- check the dist folder.
                    -- `node ./dist/index.js`
                        -- to execute this. 

                    -- add start and dev script in package.json file
                        -- on some places we use     `"dev": "npx ts-node src/index.ts"`
                    
                    