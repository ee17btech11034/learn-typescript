function greet2(name: string): string { // here we are expecting name as a string and return an string as well
    return ("Hello "+ name)
}

console.log(greet2("Raj")); // is valid as input is string

// console.log(greet2(42)); // it will throw an warning

